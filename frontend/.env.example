# Copy to .env and fill in the same value as the backend's API_KEY
# (see ../.env.example). See services/api.ts for the security tradeoff
# note on why this is acceptable for this MVP but not for a public
# production deployment.
VITE_API_KEY=
